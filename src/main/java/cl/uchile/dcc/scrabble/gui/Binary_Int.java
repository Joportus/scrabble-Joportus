package cl.uchile.dcc.scrabble.gui;

public interface Binary_Int {
    integers transform_to_integers();
    binary transform_to_binary();
    binary sum_a_binary(binary B);
    binary multiply_to_binary(binary B);
    binary divide_a_binary(binary B);
    binary substract_to_binary(binary B);
}
